{{/*
This file is part of Shared Library Helm Chart <https://github.com/StevenJDH/helm-charts>.
Copyright (C) 2023-2026 Steven Jenkins De Haro.

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

     http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/}}

{{/*
Generates a ServiceAccount for a CRD delete Job that is compatible with all supported
Kubernetes API versions.

Usage:
{{- include "shared-library.crd-delete-serviceaccount" . }}

OR

include "shared-library.crd-delete-serviceaccount" (dict
  "context" .
  "config" .Values.crds
)

OR

{{- include "shared-library.crd-delete-serviceaccount" (dict
  "context" .
  "config" .Values.crds
  "override" "override.crd-delete-serviceaccount"
) }}

{{- define "override.crd-delete-serviceaccount" -}}
{{- $ctx := .context -}}
metadata:
  labels:
    foo: bar
    {{- include "shared-library.labels" $ctx | nindent 4 }}
{{- end -}}

NOTE: This template is meant to be used by the 'shared-library.crd-delete-orch'
      template, which wraps the five needed templates into one. If customization
      is needed, then do not use the orchestrator.

Params:
  - context - Dict - Required. The top-level context for the template evaluation.
  - config - Dict - Required. The context for the 'crds' config section.
  - override - String - Optional. Definition to merge into the included template.
*/}}

{{- define "shared-library.crd-delete-serviceaccount.tpl" -}}
{{- $ctx    := .context -}}
{{- $config := .config  -}}
{{- if $config.deleteJob.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "shared-library.fullname" $ctx }}-crd-delete
  annotations:
    "helm.sh/hook": post-delete
    "helm.sh/hook-weight": "-20"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
  labels:
    {{- include "shared-library.labels" $ctx | nindent 4 }}
  namespace: {{ $ctx.Release.Namespace }}
{{- end }}
{{- end -}}

{{- define "shared-library.crd-delete-serviceaccount" -}}
{{- if and (kindIs "map" .) (eq (len .) 3) (hasKey . "override") .override }}
  {{- $ctx := omit . "override" -}}
  {{- include "shared-library.util.merge" (list $ctx .override "shared-library.crd-delete-serviceaccount.tpl") }}
{{- else if and (kindIs "map" .) (eq (len .) 2) (hasKey . "config") -}}
  {{- include "shared-library.crd-delete-serviceaccount.tpl" . }}
{{- else -}}
  {{- include "shared-library.crd-delete-serviceaccount.tpl" (dict
    "context" .
    "config" .Values.crds
  ) }}
{{- end }}
{{- end -}}